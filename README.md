# Nuna Tools (Streamlit)

Repo contoh untuk deploy ke Streamlit Community Cloud.

## Jalankan lokal
```bash
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
streamlit run streamlit_app.py
```

## Multi-page
Halaman tambahan ada di folder `pages/`.
